let money = 'or die',
    income = 10,
    addExpenses = 10,
    deposit = 30,
    mission = 'improve your',
    period = 'six weeks';

alert(mission + ' JS for ' + period);
console.log(money);
